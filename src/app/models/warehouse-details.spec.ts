import { WarehouseDetails } from './warehouse-details';

describe('VehicleDetails', () => {
    it('should create an instance', () => {
        // @ts-ignore
        expect(new WarehouseDetails()).toBeTruthy();
    });
});